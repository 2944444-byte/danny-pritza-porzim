# Production build settings (used by `vite build` on Vercel).
# Same-origin API: the frontend calls /api/* which Vercel routes to the
# Python serverless function (see vercel.json + api/index.py).
VITE_API_BASE_URL=/api
